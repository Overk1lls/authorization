export const NotFound = () => (
    <header>
        <div className="text-center">
            <h1>
                {window.location.pathname} page is not found!
                <br />
                :(
            </h1>
        </div>
    </header>
);